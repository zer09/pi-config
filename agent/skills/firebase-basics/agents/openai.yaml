interface:
  display_name: "Firebase Basics"
  short_description: "Set up Firebase CLI login and projects"
  default_prompt: "Use $firebase-basics to configure Firebase CLI login, project selection, local initialization, or app config files, not application user sign-in."
