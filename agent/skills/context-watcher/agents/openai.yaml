interface:
  display_name: "Context Watcher"
  short_description: "Route commands through Context Mode"
  default_prompt: "Use $context-watcher to route shell work, large output, graph-first code exploration, tests, logs, docs, and GitHub operations through Context Mode, RTK, and CodeGraph. List live CodeGraph tools before optional graph-tool assumptions because visibility is gated by the server's active/default project size."
