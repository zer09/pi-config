interface:
  display_name: "Firebase Firestore"
  short_description: "Design and inspect Cloud Firestore"
  default_prompt: "Use $firebase-firestore to design models, queries, rules, or indexes, inspect databases, or provision an explicitly requested database."
