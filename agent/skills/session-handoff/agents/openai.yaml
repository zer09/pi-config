interface:
  display_name: "Session Handoff"
  short_description: "Create or verify project handoffs"
  default_prompt: "Use $session-handoff to save this work in a project-local handoff for a future session."
