interface:
  display_name: "Session Handoff"
  short_description: "Create session handoff docs"
  default_prompt: "Use $session-handoff to create a concise handoff document that lets a future agent resume this work without ambiguity."
