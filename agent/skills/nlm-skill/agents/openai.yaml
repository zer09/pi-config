interface:
  display_name: "Gemini Notebook / NotebookLM CLI"
  short_description: "Use NotebookLM through nlm or MCP"
  default_prompt: "Use $nlm-skill to inspect the selected NotebookLM notebook and summarize its sources."

dependencies:
  tools:
    - type: "mcp"
      value: "notebooklm-mcp"
      description: "NotebookLM MCP server"
