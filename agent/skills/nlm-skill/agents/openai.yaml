interface:
  display_name: "Gemini Notebook / NotebookLM CLI"
  short_description: "Automate Gemini Notebook with nlm and MCP"
  default_prompt: "Use $nlm-skill to create a Gemini Notebook workflow, add sources, and generate the requested artifact."

dependencies:
  tools:
    - type: "mcp"
      value: "notebooklm-mcp"
      description: "NotebookLM MCP server"
