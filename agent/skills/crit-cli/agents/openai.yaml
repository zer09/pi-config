interface:
  display_name: "Crit CLI"
  short_description: "Read Crit reviews and manage requested CLI actions"
  default_prompt: "Use $crit-cli to read unresolved local review comments without changing review state."
