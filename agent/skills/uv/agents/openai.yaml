interface:
  display_name: "uv"
  short_description: "Use the uv Python package manager"
  default_prompt: "Use $uv to resolve this uv command, configuration, dependency, or environment task while preserving the project's package-manager conventions."
