interface:
  display_name: "Codebase Memory MCP"
  short_description: "Use the local code knowledge graph"
  default_prompt: "Use $codebase-memory-mcp to explore the codebase graph before falling back to grep."

dependencies:
  tools:
    - type: "mcp"
      value: "codebase-memory-mcp"
      description: "Local code knowledge graph MCP server"
