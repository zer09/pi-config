interface:
  display_name: "Create Design System Rules"
  short_description: "Author reusable project Figma-to-code rules"
  default_prompt: "Use $figma-create-design-system-rules to author reusable Figma-to-code conventions in this project's existing rule format and location."

dependencies:
  tools:
    - type: "mcp"
      value: "figma"
      description: "Figma MCP server"
      transport: "streamable_http"
      url: "https://mcp.figma.com/mcp"
