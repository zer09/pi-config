interface:
  display_name: "Notion"
  short_description: "Search, read, write, and organize Notion"
  default_prompt: "Use $notion to read the requested workspace content or perform only my explicitly requested Notion write through ntn."
