# ADR 0007: Isolate delegated solution, implementation, and review roles

## Status

Accepted for role isolation and safety policy. The Local Skill and Python supervisor mechanism is superseded by ADR 0008. ADR 0009 additionally supersedes the following parts of this ADR:

- **Route maps and provider counts.** Every route map, member chain, eligible-provider set, and provider count in the Decision section, including the A/B/C/D backup maps and the five-provider Codex alias sets for D and the Oracle, is superseded by the extension-owned `routing.json`; the current authority, including the current provider counts, is ADR 0009. The maps below remain factual provenance of the route intent that configuration encodes.
- **Public backend schema and terminology.** The routine `backend` tool parameter, its public values `default` and `zai`, and every `backend=zai` and `backend=claude` selection rule are removed; the exceptional `routingOverride` replaces them.
- **Pre-tool-only fallback cutoff.** The guarded wrapper's rule that fallback stops permanently at the first tool execution or terminal delegate result is superseded by ADR 0009's operational-failure rule with the fixed sanitized restart note.
- **Dependent normative sections.** Decision, Consequences, and Validation statements that depend on the superseded mechanisms, including backup-only backend status, per-map primary guarantees for grouped tiers such as Gate B's OpenCode Go primary, the pre-tool advancement rule, and the backend-schema checks, no longer state current policy.

The dated Context history, including every incident narrative, remains factual provenance and is unchanged.

## Context

A successful implementation workflow used the current Pi session as an orchestrator and fresh one-shot Pi processes as solution-investigation, implementation, independent-review, finding-verification, and focused-remediation delegates. The same role isolation applies whenever Z.AI GLM 5.3 or Claude Code with Claude Opus 5 serves an assigned role. The useful behavior came from more than merely asking a second model for help:

- solution investigation, implementation, and review used fresh role-appropriate processes;
- each role started without conversation history;
- only one delegate could mutate the shared working tree;
- independent reviewers received governing documents but no remediation steering;
- blocking findings were verified independently before a separate remediation run;
- every remediation was followed by another fresh review;
- project execution guides remained authoritative; and
- Git and hosted-service transitions remained separately authorized.

Reconstructing these constraints ad hoc is error-prone. Putting the full procedure in global `AGENTS.md` would increase every session's context cost. Prompt templates require explicit user invocation and do not reliably route implicit orchestration requests. A Pi extension could automate process management, but would introduce executable lifecycle, cancellation, output-capture, and mutation-safety complexity before it is needed.

The previously retired `context-watcher` was a broad orchestration runtime skill. Restoring that broad scope is unnecessary; this decision concerns one narrow, explicitly requested implementation/review loop.

Repeated delegate calls exposed a missing lifecycle bound. One delegate remained silent for about 85 minutes and ended only after SIGTERM with no report. A later retry left no tool result when the parent session ended. Another browser verifier stayed active for two hours although its remaining proof was already blocked. Fresh sessions and role isolation do not protect against provider stalls, silent tools, active but unproductive loops, empty reports, or abandoned descendants.

A 2026-08-16 external chart review supplied only directional evidence that lower reasoning effort can reduce latency. Its subscription estimates and Pareto curve were not reliable enough to establish model policy. After a brief lower-effort trial decision, the user selected GLM 5.3/max as the implementation default and retained Luna/xhigh only for positively classified small tasks. Final independent review retains higher effort because missed blockers have greater cost than slower execution.

AgentRouter, SeekAI, and GoRouter later supplied useful additional routes, but their catalog or provider availability can be intermittent. A live check initially reported SeekAI and GoRouter as unavailable because the existing Pi process predated their API-key environment variables. Fresh supervised smokes succeeded after Pi restarted from a refreshed shell. Catalog availability therefore reflects both provider health and the credentials inherited when the parent Pi process starts. A later SeekAI Fable 5 review ended with provider status `client_gone`, error `context canceled`, five completed tool calls, and no final report. Those gateway signals indicate provider unavailability, but completed tool work still makes automatic replacement unsafe. Blind automatic retries are unsafe for mutating delegates because a prior attempt may already have changed the shared tree. A safe route fallback therefore needs a strict pre-tool boundary, fresh processes, ordered one-shot candidates, and one shared deadline.

On 2026-08-17, one OpenAI Codex delegate and one AgentRouter delegate each produced a valid `COMPLETED` report, final `agent_end`, and `agent_settled`. Pi remained alive after both terminal lifecycles, so the supervisor waited exactly the 600-second idle window and mislabeled both results as `stalled`. The reports, empty stderr logs, and clean streams proved a provider-independent process-shutdown problem. Terminal lifecycle completion must therefore take precedence over later process idleness while process-group cleanup remains mandatory.

On 2026-08-19, another Pi JSON delegate returned the provider's outage notice as the complete assistant report text, `[error] Service temporarily unavailable, please try again later`, after a settled lifecycle with zero tools and no structured marker. The supervisor stored that text as the final report, recorded `invalid_result`, and left the availability signal unset, so the guarded route could not apply its existing pre-tool failover. The correction recognizes exactly that single-line machine envelope as provider unavailability, while multi-section reports and every other missing-marker report remain terminal failures.

Later on 2026-08-19, a SeekAI Fable 5 review delegate ended after three agent cycles, five completed tool calls, and a clean final lifecycle with a substantial report that lacked the structured marker: `invalid_result` with `route_unavailable_seen` false. The provider dashboard showed two failed requests ending with reason `scanner_error` and error `unexpected EOF`, consistent with Pi retry cycles, but the raw Pi JSON had already been deleted, so the dashboard could not prove which local typed error field carried those strings. The correction adds those two narrow signatures to the existing availability classifier for the typed error fields it already scans. The five-tool boundary stays absolute: such an attempt remains a terminal `invalid_result` failure because the chain's pre-tool cutoff outranks the availability boolean, and no new provider text is persisted.

For problems without an accepted implementation plan, parent-only diagnosis can commit too early to one interpretation. Three independent read-only investigators can first locate the relevant flow, cite exact source evidence, and propose solutions. The parent remains the orchestrator: it verifies those claims, resolves non-architectural differences, and finalizes the solution contract. Investigators cannot later provide independent approval of the implementation they influenced, so post-implementation review uses completely fresh processes.

On 2026-08-20, the user replaced every remaining active SeekAI role route with Tabitoken. Tabitoken maps no `xhigh` thinking level for Claude Opus 4.8 Thinking, and the small-task primary deliberately stays below the mapped `max` level to preserve the lower-cost purpose of this narrowly classified route, so it runs at `high` while its AgentRouter and Luna fallbacks keep `xhigh`. `agent/models.json` has no configured Fable-equivalent model. Live-catalog verification of Tabitoken routes remains pending until Pi restarts with `TABITOKEN_API_KEY` set. Review A therefore uses Claude Opus 4.8 Thinking/high, which also avoids duplicating Investigator A's Opus 5 model. A small-task implementation can now be reviewed through the same Tabitoken Opus 4.8 Thinking route, so the implementer and reviewer share a model route. Review independence still depends on a fresh isolated reviewer process and the paired AgentRouter reviewer, not on guaranteed model separation; this overlap is model-route overlap, not process reuse, and a delegate process still cannot review its own work. Restoring model separation requires a future explicit route-policy decision. Tabitoken has no configured DeepSeek model, so the former DeepSeek fallback was removed rather than replaced with an invented route. The earlier SeekAI incident narratives remain factual provenance for the availability classifier and route-fallback design.

Later on 2026-08-20, the user expanded both conditional solution investigation and post-implementation independent review from two members to three: A, B, and C. A primaries on CGPT3 GPT-5.6 Luna/max, B on OpenCode Go DeepSeek V4 Flash/max, and C on OpenCode Go HY3/high. HY3 maps no `max` thinking level, so Pi would clamp `max` to `high`; the user selected HY3 at `high` explicitly. Investigation and review share the same primary assignments, and each member carries one ordered backup map covering SeekAI, AgentRouter, Tabitoken, and GoRouter routes. Those four backends are backup-only and never start as primaries in the default policy. Every gate requires all three reports; one or two surviving reports are useful evidence but cannot complete the gate. The user also raised finding verification from Sol/medium to Sol/high and retired the Tabitoken-first small-task implementation/remediation chain: narrow tasks now route to GLM 5.3/max like all other implementation work, and explicit user or project backend selection remains available with role-based permissions.

On 2026-08-21, the user replaced the A primary for both conditional solution investigation and independent review: OpenCode Go Muse Spark 1.2 Contributor at `xhigh` replaces CGPT3 GPT-5.6 Luna at `max`, and the SeekAI GPT-5.6 Luna backup leaves the A map. Muse Spark 1.2 Contributor maps `max` to null in its local thinking-level map, so `xhigh` is its highest supported thinking level. A now backs up through AgentRouter GPT-5.6 Sol/max, Tabitoken Claude Opus 5 Thinking/max, SeekAI Claude Opus 5/max, and GoRouter Claude Opus 5 Thinking/high. The B and C maps, the three-member gates with all-three completion semantics, the backup-only status of the four fallback backends, and the GLM 5.3/max implementation default are unchanged.

Later on 2026-08-21, the user expanded both conditional solution investigation and post-implementation independent review from three members to four by adding D. D always uses GPT-5.5 at thinking `medium` under backend=default. D's eligible provider set is exactly five OpenAI Codex alias providers, `openai-codex`, `openai-codex-zahlo`, `openai-codex-cgpt1`, `openai-codex-cgpt2`, and `openai-codex-cgpt3`; Cursor is excluded. D reads the parent session's currently selected provider from native extension context rather than environment variables. When that provider is eligible it becomes D's primary; otherwise one eligible provider is selected at random. The draw happens exactly once per `delegate_run` invocation, and the remaining four providers follow in stable canonical order. Catalog preflight still skips unavailable routes, explicit backend=zai or backend=claude still overrides default D routing with assigned-role permissions, and the A/B/C route maps plus all implementation, remediation, and verification routes are unchanged. Gates now require all four reports, and the parent synthesizes evidence rather than voting.

Also on 2026-08-21, the user replaced the C primary for both conditional solution investigation and independent review: OpenCode Go Ox Alpha Free at `max` replaces OpenCode Go HY3 at `high`. HY3 mapped no `max` thinking level, so the former C primary ran at `high` because Pi would clamp an unmapped `max`; Ox Alpha Free maps `max` to `max` in its local thinking-level map, so the new C primary runs at `max` natively. The C ordered backup map, the A/B/D route maps, the four-member gates with all-four completion semantics, and every implementation, remediation, and verification route are unchanged. The earlier HY3 paragraphs remain factual provenance for the former C policy.

Later still on 2026-08-21, the user added a conditional Sol oracle stage between solution investigation and implementation. The oracle is a distinct `oracle` role: read-only, advisory, and exclusive like finding verification. It runs exactly once, only after a required solution A/B/C/D gate completed, the parent verified evidence, and the parent synthesized a draft solution contract; a small task that skips solution investigation also skips the oracle. Main-Sol skip detection is model-id based rather than provider based: when the parent session's current model id is exactly `gpt-5.6-sol`, the oracle is skipped and the parent finalizes the solution contract directly, on any parent provider. The oracle always uses `gpt-5.6-sol` at thinking `high` through default Pi routing on exactly the five D-eligible OpenAI Codex alias providers in canonical order, with the same inherited-eligible-or-one-random primary selection as D; Cursor, AgentRouter, SeekAI, and every other provider are excluded, and explicit backend=zai or backend=claude is invalid for the oracle and is rejected before spawning. The extension enforces both gates defensively before any child process exists, without fabricating an oracle report. The parent passes the oracle the neutral problem, governing documents, verified evidence, the draft solution contract, constraints, and unresolved uncertainties, never raw investigator reports or synthesis rationale. The oracle returns exactly one verdict, `VALID` or `REVISE`, with correctness analysis, missing invariants and risks, better alternatives where material, exact path:line evidence, validation changes, and limits; it never edits, mutates Git or hosted state, implements, or starts delegates. Oracle claims are verified like any other evidence: the parent revises the draft when warranted and finalizes the contract before implementation. The oracle is advisory, not the final authority, there is no automatic oracle loop, and a non-completed oracle run blocks implementation.

On 2026-08-22, the user restored the C primary for both conditional solution investigation and independent review back to OpenCode Go HY3 at `high`: HY3's live catalog entry still maps `max` to null in its local thinking-level map, so the restored C primary runs at `high`, the highest mapped level, because Pi would clamp an unmapped `max`. The C ordered backup map, the A/B/D route maps, the conditional Sol oracle routing and skip behavior, the four-member gates with all-four completion semantics, and every implementation, remediation, and verification route are unchanged. The earlier Ox Alpha Free paragraph remains factual provenance for the interim C policy.

Later on 2026-08-22, the user moved independent finding verification from strictly sequential to bounded parallelism. Exact duplicate findings are consolidated before launch, each verification receives exactly one finding without sibling verification reports, and independent verifications run concurrently in batches of at most four; dependent findings stay sequential when one verification result materially affects another finding's contract or evidence. The parent waits for every required verification in the current batch before remediation: a failed or non-completed verification leaves its finding unresolved without erasing completed sibling reports, and only verification-confirmed findings reach the one mutating remediation delegate followed by a fresh four-reviewer gate. Verification still overlaps no solution, review, implementation, remediation, or oracle role in either start order, still uses `openai-codex/gpt-5.6-sol:high`, and still receives pre/post Git fingerprints. The manager enforces the four-verification cap with a bounded batching error. The earlier sequential-verification sentences remain factual provenance for the skill and first-extension eras.

Later still on 2026-08-22, the user removed the global pre/post Git tree fingerprints and the `read_only_mutation` state from the native extension. Shared monorepo worktrees are modified concurrently by unrelated agents, so a before/after tree fingerprint cannot attribute the actor and incorrectly invalidated otherwise completed read-only reports. Read-only roles stay enforced through their role contracts and permission classification. The earlier fingerprint sentences in this document remain factual provenance for the skill and first-extension eras.

On 2026-08-22, the native extension migrated every route attempt to one persistent Pi RPC child and removed the direct Claude Code CLI backend. A clean settled missing or invalid report now receives exactly one fixed recovery prompt in the same child session. Structured provider failures, including credit, quota, billing, authentication, rate-limit, and availability failures, are classified before recovery; pre-tool failures may use the existing route fallback, while failures after tools or accepted recovery fail closed. RPC prompt rejection is the distinct `prompt_rejected` state. Claude-named models served through Tabitoken, SeekAI, AgentRouter, GoRouter, and other ordinary Pi providers remain Pi routes and are unchanged. The direct `backend=claude`, Claude route type, `claude --print` supervisor, permission arguments, and plain protocol were removed without invoking or modifying the user's Claude CLI.

On 2026-08-23, after the routing policy moved into the extension-owned `routing.json` configuration defined by ADR 0009, the user expanded post-implementation independent review from four members to five by adding E on the dedicated single-tier `gate-e` route `openai-codex-cgpt5/gpt-5.6-sol` at thinking `high`, selected through the same shared config-driven selector with no code special case. Later the same day the user reverted the persistent fifth reviewer: the default review gate is four-member A/B/C/D again, and the `review-e` role and `gate-e` profile are removed while the role taxonomy stays fixed and fail-closed because roles define permission and concurrency classes. When one gate temporarily needs an extra reviewer, that reviewer reuses an existing non-exclusive review role with a distinct prompt, and an optional reason-required one-run `routingOverride` gives it a distinct route for that run, because the manager already admits duplicate non-exclusive review roles. The A/B/C/D review routes, the solution gate, the oracle, verification, and implementation routes are unchanged.

On 2026-08-24, the user added a narrow reviewer-gate waiver to the strict all-four completion rule. Review delegates can fail operationally or end non-completed, including `blocked`, `cleanup_failed`, and `routes_unavailable` outcomes, even after the routing chain's bounded fallback, which left a gate unable to finish although its surviving reports were complete. The waiver stays narrow and user-controlled: only the user may explicitly waive named failed reviewer roles for the one current gate; the parent then continues with the completed reports, records which reviewers were waived and that the gate completed under user waiver, never labels a waived failure as a reviewer pass, and changes no later gates, roles, routing, or concurrency. The waiver does not dismiss findings from completed reviewers, does not waive finding verification, remediation, oracle exclusivity, interruption handling, or other safety rules, and generic requests to continue, commit, or skip retries do not create one. No runtime extension code enforces gate completion, so the policy is encoded in the model-visible gate guidance and global instructions.

Also on 2026-08-24, the user added a typed terminal reason contract for non-completed delegates. A bare `BLOCKED` or `FAILED` marker left the parent without any bounded signal for why a delegate stopped, which mattered once waivers made non-completed reviewer outcomes actionable. New responses must carry one strict reason line, `DELEGATE_REASON: <code>`, directly above the final marker, drawn from closed BLOCKED and FAILED enums. The parser accepts only exact fixed codes, discards unknown, malformed, duplicate, misplaced, path-like, credential-like, overlong, Unicode, or outcome-mismatched values, and never persists or renders delegate-authored free text: an intentional BLOCKED or FAILED stays terminal with reason `unspecified` plus status `rejected` (or `missing` for legacy bare markers), and a reason line paired with COMPLETED follows the existing invalid-result recovery path. A missing or rejected reason never triggers recovery or route fallback, and a `blockedMisuseSuspected` flag marks BLOCKED runs whose accepted reason is `finding_reported`, because reviews with findings must use COMPLETED. The reason, status, and flag propagate through snapshots, attempt statuses, progress, the runner result, ToolResult details, and failure diagnostics (schema 4).

Later on 2026-08-24, the user extended the same waiver discipline to the solution-investigation gate, mirroring the reviewer waiver. Solution investigators can fail operationally or end non-completed, including `blocked`, `cleanup_failed`, and `routes_unavailable` outcomes, even after the routing chain's bounded fallback, which left a required solution gate unable to finish although some reports were complete. The waiver stays narrow and user-controlled: only the user may explicitly waive named failed solution roles for the one current solution gate; the parent then continues synthesis using only the completed solution reports plus parent-verified repository evidence, records which solution roles were waived and that the solution gate proceeded under user waiver, never labels a waived failure as completed or passed, and changes no later solution gates, roles, routing, or concurrency. At least one solution delegate must have completed; the user cannot waive the entire evidence set and synthesize from zero completed investigator reports. The waiver does not fabricate or dismiss evidence, resolve uncertainties, authorize implementation, replace parent evidence verification, skip the advisory oracle when otherwise required, or weaken implementation, review, verification, or remediation rules, and generic requests to continue, commit, or skip retries do not create one. No runtime extension code enforces gate completion, so the policy is encoded in the model-visible gate guidance and global instructions.

Later on 2026-08-24, the user expanded the required solution-investigation gate from A/B/C/D to A/B/C/D/E. Solution E is a permanent read-only role using the generic `solution-*` role contract; its GPT-5.6 Sol/high provider pool is defined by ADR 0009. The five investigators launch concurrently with the same neutral assignment and the existing named-role waiver rules apply unchanged.

## Decision

Adopt a layered delegated-Pi workflow:

1. Keep only the trigger and non-negotiable safety rules in global `agent/AGENTS.md`.
2. Keep the runtime workflow in the progressively disclosed `delegated-pi-loop` Local Skill.
3. Keep exact generic spawn commands and role prompt contracts in the skill's on-demand reference.
4. Keep provenance, update checks, and evaluation guidance in `docs/skills/delegated-pi-loop-update-process.md`.

The parent Pi session is the sole orchestrator. Spawned Pi RPC delegates execute their assigned role directly and do not recursively delegate.

Route every child through the native `delegated-pi-loop` supervisor. Each route attempt starts one ephemeral Pi process with `--mode rpc --no-session --approve`, keeps stdin open, and sends the assigned task as correlated `prompt-1`. The defaults remain a shared 45-minute wall deadline, cumulative 50 MiB protocol/output limit, five-minute event-idle warning, and ten-minute event-idle termination. The TypeScript supervisor privately parses thinking, text, tool, message, turn, and agent events; updates only bounded activity metadata; classifies provider failure; extracts only the authoritative report; and never persists raw protocol events. A clean settled `missing_report` or `invalid_result` receives exactly one fixed `prompt-2` in the same child session. The extension does not merge reports or insert a marker. A valid `COMPLETED` report followed by final `agent_end`, `agent_settled`, and a clean stream ends successfully even if Pi remains alive. Process-group cleanup remains mandatory.

Documented multi-provider Pi roles use a guarded route wrapper. The wrapper preflights each exact provider/model against Pi's currently available catalog. An ordered chain can advance after catalog absence, recognized provider unavailability such as `client_gone`, `context canceled`, `scanner_error`, or `unexpected EOF`, a machine-rendered single-line `[error]` envelope that forms the complete final assistant report, or an event-idle stall only before any tool execution and without a terminal delegate result. Each candidate starts in a fresh process, receives the same role prompt, and runs within the original shared wall deadline. The wrapper never cycles to an earlier route and never exposes failed-route raw events or provider errors to the parent model. Provider credentials pass unchanged from the parent Pi environment; newly configured variables require a parent Pi restart from a refreshed shell. Delegate commands do not source shell startup files or expose credential values. Clear inherited `PI_SESSION_ID`, `PI_SESSION_FILE`, `PI_PROVIDER`, `PI_MODEL`, and `PI_REASONING_LEVEL` before each delegate. Run mutators sequentially, and run independent finding verifiers only in bounded verification-only batches. Run each documented read-only gate concurrently in separate direct bash tool calls: the five-member solution investigators before implementation when needed, then the four fresh independent reviewers after implementation. Use fresh ephemeral processes:

- Pi: `--mode rpc --no-session --approve` through strict LF-framed RPC JSONL.

Pi 0.84.1 sets `AI_AGENT=pi` and `PI_CODING_AGENT=true` in each Pi child. Pi's built-in bash tool replaces the cleared metadata with the child session's values. The scrub prevents parent metadata from misleading child extensions or generic subprocesses.

Default role assignments are:

- independent solution investigation A: `opencode-go/muse-spark-1.2-contributor`, thinking `xhigh`, because that model's thinking-level map maps `max` to null and `xhigh` is its highest supported level, with the ordered backup map `agentrouter/gpt-5.6-sol` → `tabitoken/claude-opus-5-thinking` → `seekai/claude-opus-5`, all at `max`, then `gorouter/claude-opus-5-thinking` at `high`;
- independent solution investigation B: `opencode-go/deepseek-v4-flash`, thinking `max`, with the ordered backup map `seekai/deepseek-v4-flash` → `agentrouter/claude-opus-5` → `tabitoken/claude-opus-5-thinking`, all at `max`, then `gorouter/claude-opus-5-thinking` at `high`;
- independent solution investigation C: `opencode-go/hy3`, thinking `high`, because HY3 does not support `max` and Pi would clamp `max` to `high`; the user selected HY3 at `high`. Its ordered backup map is `agentrouter/claude-opus-5` → `tabitoken/claude-opus-5-thinking` → `seekai/claude-opus-5`, all at `max`, then `gorouter/claude-opus-5-thinking` at `high`;
- independent solution investigation D: `gpt-5.5`, thinking `medium`, on an ordered member chain of exactly five eligible providers, `openai-codex`, `openai-codex-zahlo`, `openai-codex-cgpt1`, `openai-codex-cgpt2`, and `openai-codex-cgpt3`, never Cursor. The primary is the parent session's currently selected provider when it is eligible, otherwise one randomly selected eligible provider drawn exactly once per `delegate_run` invocation; the other four follow in the stable canonical order listed here;
- independent solution investigation E: the current `gate-e` profile from ADR 0009, using the same generic read-only solution contract and shared selector as A/B/C/D;
- solution oracle: `gpt-5.6-sol`, thinking `high`, on an ordered member chain of the same five eligible providers with the same inherited-or-random primary selection as D. Run exactly once against the synthesized draft solution contract after a required solution gate; skipped by model id when the parent session's current model is exactly `gpt-5.6-sol` on any provider; read-only and exclusive; advisory `VALID` or `REVISE`; explicit Z.AI is invalid for the oracle and is rejected before spawning;
- implementation and focused remediation: `zai/glm-5.3`, thinking `max`;
- independent review A, B, C, and D: the same four route maps as solution investigation;
- finding verification: `openai-codex/gpt-5.6-sol`, thinking `high`;
- explicit Z.AI alternative for any assigned role: `zai/glm-5.3`, thinking `max`, with mutation permissions inherited from the role.

The public backend values are exactly `default` and `zai`. Pi provider routes whose model IDs contain `claude` remain supported and retain their exact provider, model, thinking, and ordering.

AgentRouter, Tabitoken, SeekAI, and GoRouter are backup-only backends: they appear only inside member backup maps and never start as primaries in the default policy. The former Tabitoken-first small-task implementation/remediation chain is retired; explicit user or project backend selection remains allowed with role-based permissions.

When a problem lacks an accepted solution contract, default solution investigation launches all five read-only investigators A, B, C, D, and E concurrently with the same neutral prompt. Each member's chain may advance through its ordered backup map one fresh pre-tool attempt at a time within one shared deadline. All five reports remain isolated. The orchestrator verifies cited source and architecture evidence, compares the proposals, and synthesizes a draft solution contract. The only exception to all-five completion is an explicit user waiver: after one or more solution delegates fail operationally or end non-completed, the user may waive the named failed solution roles for that one current solution gate; the parent then continues synthesis using only the completed solution reports plus parent-verified repository evidence, records which solution roles were waived and that the solution gate proceeded under user waiver, never labels a waived failure as completed or passed, and changes no later solution gates, roles, routing, or concurrency. At least one solution delegate must have completed, because the user cannot waive the entire evidence set and synthesize from zero completed investigator reports. The waiver does not fabricate or dismiss evidence, resolve uncertainties, authorize implementation, replace parent evidence verification, skip the advisory oracle when otherwise required, or weaken the later implementation, review, verification, or remediation rules, and a generic request to continue, commit, or skip retries does not create one; precise user wording such as "solution C may be waived for this gate" authorizes only that named waiver. Unless the parent session itself runs exactly `gpt-5.6-sol`, one fresh read-only oracle then reviews that draft contract before it is finalized; the parent verifies the oracle verdict, revises when warranted, and finalizes. Material architecture ambiguity stops for user input. Investigators cannot implement, and neither investigators nor the oracle can serve as the later reviewers.

GLM 5.3/max is available for any assigned role and remains the implementation and remediation default for every task size, because the small-task chain is retired. Z.AI investigation, review, or verification requires explicit user or project selection and remains read-only. The assigned role, not the backend, controls mutation permissions. Default independent review launches the four fresh read-only reviewers A, B, C, and D concurrently after implementation, with the same route maps and fallback rules as investigation. All four reviews must complete, and findings from any report require processing. The only exception is an explicit user waiver: after one or more reviewers fail operationally or end non-completed, the user may waive the named failed reviewer roles for that one current gate; the parent then continues with the completed reports, records which reviewers were waived and that the gate completed under user waiver, never labels a waived failure as a reviewer pass, and changes no later gates, roles, routing, or concurrency. The waiver does not dismiss findings from completed reviewers or waive finding verification, remediation, or other safety rules, and a generic request to continue, commit, or skip retries does not create one; precise user wording such as "C may be waived for this gate" authorizes only that named waiver. A temporary extra reviewer for one gate reuses an existing review role with a distinct prompt and an optional reason-required one-run routing override, because duplicate non-exclusive review roles are admitted and the role taxonomy stays fixed. An explicit Z.AI reviewer selection does not silently reduce a required four-reviewer gate. OpenAI Codex Sol/high remains the finding-verification default. Pin Pi model and thinking identifiers rather than using moving aliases. User instructions and more-specific project workflows may otherwise override model selection. Project role templates, finding taxonomies, and release gates take precedence over generic skill skeletons.

Only one mutating delegate may run on a shared working tree, and the parent must not edit concurrently. Solution investigators, reviewers, and verifiers are read-only and neutral through their role contracts and permission classification; global tree fingerprints are no longer captured, so concurrent unrelated tree changes do not invalidate their reports. Every role prompt defines attempt/time budgets and ends with one structured `DELEGATE_RESULT` marker. A `BLOCKED` or `FAILED` marker carries exactly one `DELEGATE_REASON` line directly above it with one exact code from the closed BLOCKED or FAILED enum; the parser accepts only those fixed codes, discards every other value as rejected, keeps a bare legacy marker terminal as missing, and never persists or renders delegate-authored reason text. A delegate that cannot establish a required result within its budget reports `BLOCKED` with its reason code and stops unrelated work. If a project provides separate solution-investigation, finding-verification, or focused-remediation templates, instantiate each in separate fresh processes. Parent-session analysis cannot replace required independent verification.

Do not stage, commit, push, open or merge pull requests, deploy, or mutate hosted services unless that exact transition has been separately authorized.

## Consequences

- Other Pi sessions can reproduce five-member concurrent pre-implementation solution investigation, parent evidence verification and synthesis, default GLM 5.3/max implementation, fresh four-member concurrent independent review with backup-only fallback maps, OpenAI Codex Sol/high verification, or explicitly selected Z.AI assignments from a compact trigger.
- The always-loaded global context grows only by the short trigger/safety section; detailed behavior remains on demand.
- Independent-review credibility depends on role and context isolation rather than model self-approval.
- Shared-tree safety relies on role contracts and permission classification, not on an operating-system sandbox: Pi-based read-only delegates still receive `bash` and other writable tools, and the extension no longer detects their mutations automatically.
- A Pi delegate with no valid activity event for ten minutes reaches `stalled`; an active loop still reaches the 45-minute wall deadline. A valid completed lifecycle reaches success before either limit and cleans up any lingering process group.
- `COMPLETED`, `BLOCKED`, and `FAILED` outcomes are machine-readable, and non-completed outcomes carry one typed terminal reason from a closed enum: unknown, malformed, duplicate, misplaced, or outcome-mismatched reason values are discarded while the intentional outcome stays terminal with `unspecified`, and a reason line paired with `COMPLETED` follows the existing invalid-result recovery path. Empty reports, malformed streams, and missing markers fail explicitly.
- Supervisor artifacts preserve the final report, bounded stderr, activity metadata, and terminal status without persisting the command line or raw Pi events. Failure diagnostics use schema 4 and carry the delegate outcome, the typed terminal reason and its status, and the `blockedMisuseSuspected` flag without any delegate-authored free text, path, provider body, or credential.
- All implementation and remediation work routes to GLM 5.3/max; the former Tabitoken Claude Opus 4.8 Thinking-first small-task chain is retired from the default policy.
- Conditional solution investigation consumes five concurrent provider calls before implementation. The conditional oracle adds exactly one sequential read-only call after a required gate, skipped without cost when the parent itself runs `gpt-5.6-sol`. Final review consumes four additional fresh calls. A gate remains incomplete when any assigned delegate fails or is unavailable, unless the user explicitly waives the named failed roles for that current gate, under which the gate completes with the surviving reports and records the waiver; a solution-gate waiver additionally requires at least one completed investigator report, because the user cannot waive the entire evidence set and synthesize from zero completed investigator reports. A temporary extra reviewer adds exactly one more review call for that gate.
- Catalog checks can skip an unavailable provider without spending a delegate attempt. Runtime fallback stops permanently once any tool starts or a terminal result exists.
- A narrowly recognized single-line machine `[error]` envelope marks a route unavailable through the existing boolean only; no new error detail is persisted, and all other missing-marker reports stay terminal failures.
- Narrowly recognized typed provider stream failures such as `scanner_error` and `unexpected EOF` set that same boolean through the already-scanned typed error fields. An attempt that already executed a tool stays a terminal failure regardless of the boolean, and no provider text is persisted.
- Model identifiers, thinking levels, live catalog availability, RPC framing, and process environment markers are maintenance points. Check each contract when Pi changes them.
- Real delegate evaluations can spend provider tokens and may mutate a fixture, so structural validation is the default and live smoke tests require an appropriate disposable workspace and authorization.

## Alternatives rejected

- **Full workflow in global `AGENTS.md`:** rejected because it taxes every request and duplicates progressively disclosed material.
- **Prompt templates only:** rejected because they depend on explicit slash-command use and do not establish automatic routing for future agents.
- **Immediate Pi extension:** deferred because executable orchestration, cancellation, output capture, and sandboxing are unnecessary complexity for the mostly sequential workflow and bounded concurrent review trio.
- **Restore broad `context-watcher`:** rejected because the requested capability is narrower and should not revive unrelated orchestration behavior.
- **Repurpose solution investigators as final reviewers:** rejected because they would assess an implementation shaped by their own proposals.
- **Let implementers self-review:** rejected because it does not provide fresh context or independent judgment.
- **Keep delegates unbounded:** rejected because provider or process stalls can hold the parent tool call indefinitely and lose the final report.
- **Use only the bash tool timeout:** rejected because a dedicated supervisor provides heartbeats, durable diagnostics, explicit empty-report failure, and descendant cleanup while the outer orchestration call remains observable.
- **Retry any failed route automatically:** rejected because a mutating attempt may already have changed the shared tree. Automatic failover stops at the first tool execution or terminal delegate result.

## Validation

1. Validate the target skill and every Local Skill.
2. Parse every `agents/openai.yaml` and verify the delegated skill's default prompt names `$delegated-pi-loop`.
3. Check configured model IDs for the concurrent A/B/C/D/E solution quintet with their configured routing profiles, D's five eligible GPT-5.5/medium providers, the fresh A/B/C/D review quartet with the same maps, OpenAI Codex Sol/high verification, and Z.AI GLM 5.3/max. Separately record which routes appear in Pi's live available catalog.
4. Check that the public backend schema contains only `default` and `zai`, direct Claude CLI runtime scans are empty, and Pi-served Claude model routes retain their exact order.
5. Check global and skill instructions agree on direct bash, bounded child supervision, environment scrubbing, fresh sessions, one mutator, concurrent context-isolated solution investigators, orchestrator evidence verification and synthesis, fresh neutral reviewers, five-member solution and four-member review gate semantics, D's inherited-or-random eligible-provider primary with Cursor exclusion, and authorization gates.
6. Run supervisor and chain regressions for private Pi JSON parsing, active-event liveness, idle stalls, completed-lifecycle cleanup, malformed trailing streams, shared wall deadlines, structured outcomes, context isolation, catalog skips, pre-tool provider failover including `client_gone`, `context canceled`, `scanner_error`, `unexpected EOF`, and machine error envelopes, tool-start cutoff for the same signals, prose, multi-section, and completed-report non-matches, successful/empty reports, and descendant cleanup.
7. Check changed Markdown links, placeholders, user-specific paths, secrets, and runtime artifacts.
8. Measure the incremental global-context and skill-catalog cost without requiring paid inference.
9. When CLI/model semantics change materially, run a disposable-fixture delegate smoke only with appropriate authorization.
